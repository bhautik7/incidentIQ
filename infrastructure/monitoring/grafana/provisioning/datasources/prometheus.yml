apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    # Service name on the compose network, not a host-published port.
    url: http://prometheus:9090
    isDefault: true
    editable: false
